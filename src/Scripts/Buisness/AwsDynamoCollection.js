export default () => {

    return {
        async getCollection() {
            let response = {
                2: 1
            };
            //let cardCount = mapper.map(response);
            return response;// cardCount;
        },
        async getCardInfo() {

        }
    }
}